package ch.fhnw.richards.aigsspringserver.game;

public enum GameType {
	/*
	 * Rename those ENUM's to adhere to Java naming conventions -> not done because program API would
	 * probably break.
	 */
	TicTacToe, MineSweeper, TwentyFourtyEight;
}
